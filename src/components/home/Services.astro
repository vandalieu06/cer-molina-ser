---
import { Heart, HandHeart, House } from "@lucide/astro";
import { Image } from "astro:assets";

interface Service {
  title: string;
  description: string;
  cta: string;
  href: string;
  color: string;
  icon: any;
}

const services: Service[] = [
  {
    title: "Adopta un amigo",
    description:
      "Muchos animales esperan un hogar donde recibir cariño. Te guiaremos en todo el proceso para encontrar a tu compañero ideal.",
    cta: "Adoptar",
    href: "/adopta",
    color: "text-gold",
    icon: House,
  },
  {
    title: "Donaciones",
    description:
      "Tu ayuda económica se destina íntegramente a alimentación, medicinas y mejoras en el refugio. Cada granito de arena cuenta.",
    cta: "Información para donar",
    href: "/donar",
    color: "text-gold",
    icon: Heart,
  },
  {
    title: "Voluntariado",
    description:
      "¿Tienes tiempo libre? Únete a nuestro equipo para pasear perros, cuidar gatos o ayudar en el mantenimiento del centro.",
    cta: "Quiero ser voluntario",
    href: "/voluntariado",
    color: "text-gold",
    icon: HandHeart,
  },
];
---

<section class="py-16 bg-cream flex justify-center border-t border-cream-dark">
  <div class="w-full xl:max-w-7xl px-6">
    <div class="text-left mb-12 border-l-4 border-maroon-500 pl-6">
      <h2 class="text-3xl md:text-4xl font-bold text-maroon-900 font-serif">
        ¿Cómo puedes colaborar con nosotros?
      </h2>
      <p class="text-lg text-neutral mt-2 font-sans">
        Tu apoyo es fundamental para seguir cuidando de ellos.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
      {
        services.map((service) => (
          <div class="bg-white p-10 rounded-xl border border-cream-dark flex flex-col h-full shadow-sm hover:border-maroon-200 transition-colors">
            <div class={`${service.color} mb-6`}>
              <service.icon size={48} strokeWidth={1.5} />
            </div>

            <h3 class="text-2xl font-bold text-maroon-900 mb-4 font-serif">
              {service.title}
            </h3>

            <p class="text-neutral text-lg mb-8 grow leading-snug font-sans">
              {service.description}
            </p>

            <a
              href={service.href}
              class="inline-flex items-center justify-center gap-2 py-4 px-6 rounded-md font-bold text-white bg-maroon-500 hover:bg-maroon-600 transition-all text-lg shadow-md font-sans"
            >
              {service.cta}
              <span aria-hidden="true">→</span>
            </a>
          </div>
        ))
      }
    </div>
  </div>
</section>
