---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import { Calendar, ArrowLeft } from "@lucide/astro";

export async function getStaticPaths() {
  const blogEntries = await getCollection("blog");
  return blogEntries.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();
---

<Layout title={`${entry.data.title} | CER Molina Segura`}>
  <article class="py-20 flex justify-center bg-cream">
    <div class="w-full xl:max-w-4xl px-6">
      <a
        href={`${import.meta.env.BASE_URL}/noticias`}
        class="flex items-center gap-2 text-neutral-light hover:text-maroon-500 transition-colors mb-12 font-bold uppercase text-xs tracking-widest font-sans"
      >
        <ArrowLeft size={16} /> Volver al blog
      </a>

      <header class="mb-12">
        <div class="flex items-center gap-4 mb-6">
          <span
            class="bg-maroon-500 text-white text-xs font-bold px-4 py-1.5 rounded-full uppercase tracking-widest italic font-sans"
          >
            {entry.data.tag}
          </span>
          <div class="flex items-center text-neutral-light text-sm font-sans">
            <Calendar size={16} class="mr-2" />
            {entry.data.date}
          </div>
        </div>
        <h1
          class="text-4xl md:text-6xl font-bold text-maroon-900 tracking-tight italic leading-[1.1] font-serif"
        >
          {entry.data.title}
        </h1>
      </header>

      <div class="aspect-video mb-16 rounded-xl overflow-hidden shadow-2xl shadow-maroon-500/5">
        <img
          src={entry.data.image.src}
          alt=""
          class="w-full h-full object-cover"
        />
      </div>

      <div
        class="prose prose-lg prose-blue max-w-none
        prose-headings:italic prose-headings:font-bold prose-headings:tracking-tight prose-headings:text-maroon-900
        prose-p:text-neutral prose-p:leading-relaxed
        prose-strong:text-maroon-900"
      >
        <Content />
      </div>
    </div>
  </article>
</Layout>
